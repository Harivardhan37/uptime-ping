name: Keep Website Awake
on:
  schedule:
    - cron: '*/15 * * * *'  # every 15 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: curl -I https://n8n-9ebh.onrender.com
